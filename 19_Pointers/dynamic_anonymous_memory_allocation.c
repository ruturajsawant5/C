int main(void)
{
    /* allocate 4 anonymous bytes dynamically */

    malloc(4); 

    unsigned int N; 

    malloc(N); /* Value(N) number of bytes will be allocated dynamically
                and anonymously */

}